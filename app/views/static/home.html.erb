<% if admin_types.include?(current_user.try(:type)) %>
<%= render 'admin' %>
<% else %>
<%= render 'employee' %>
<%end%>

<%
# current_user.try(:type) == 'AdminUser' ? render('static/admin') : render('static/employee')
%>